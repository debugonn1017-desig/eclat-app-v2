-- ═══════════════════════════════════════════════════════════════════
--  権限 v6: 新権限 3 つ追加 (19 → 22)
-- ═══════════════════════════════════════════════════════════════════
--
--  追加権限:
--    1. 通知.自動配信設定 — ノルマ達成自動 Push のオン/オフ
--    2. レポート.全店ビュー — 全キャスト本指名 Excel 等、横断ビュー
--    3. 顧客.全店分析 — /admin/customer-analysis（お客様分析）+ 来店予測
--
--  方針: 全部「誰にも付与しない (デフォルト OFF)」。is_owner だけが通る。
--        UI 側で SENSITIVE_PERMISSIONS としてチェック確認ダイアログを出す。
--
--  適用: Supabase ダッシュボード → SQL Editor → 全文を貼って Run
--        既存スタッフへの自動付与は無し。手動でオン/オフ可能。
-- ═══════════════════════════════════════════════════════════════════

-- ───────────────────────────────────────────────────────
-- ① staff_permissions の CHECK 制約を更新（19 → 22 権限）
-- ───────────────────────────────────────────────────────
alter table public.staff_permissions
  drop constraint if exists staff_permissions_permission_check;

alter table public.staff_permissions
  add constraint staff_permissions_permission_check
  check (permission in (
    -- 顧客系
    '顧客.閲覧',
    '顧客.編集',
    '顧客.引継ぎ',
    '顧客.全店分析',          -- 新規 v6: お客様分析ページ + 来店予測
    -- キャスト系
    'キャスト.閲覧',
    'キャスト.アカウント管理',
    -- KPI系
    'KPI.閲覧',
    'KPI.詳細分析',
    -- シフト系
    'シフト.閲覧',
    'シフト.管理',
    -- 売上系
    '売上.閲覧',
    '売上.入力',
    -- お知らせ系
    'お知らせ.閲覧',
    'お知らせ.投稿',
    'お知らせ.管理',
    -- レポート系
    'レポート.閲覧',
    'レポート.出力',
    'レポート.全店ビュー',    -- 新規 v6: 全キャスト本指名 Excel 等
    -- 通知系
    '通知.送信',
    '通知.自動配信設定',      -- 新規 v6: ノルマ達成自動 Push の設定
    -- 設定系
    'ランク基準.設定',
    'ノルマ.設定'
  ));

-- ───────────────────────────────────────────────────────
-- ② 検証クエリ（オプション: Run 後に確認用）
-- ───────────────────────────────────────────────────────
-- select pg_get_constraintdef(c.oid) from pg_constraint c
--   join pg_class t on t.oid = c.conrelid
--   where t.relname = 'staff_permissions' and c.contype = 'c';
